#include <iostream>
#include "Tournament.h"

using namespace std;

int main()
{
    Tournament t;
    t.StartTournament();

    /*
    Board b;
    b.PlaceStone('A', 1, 'W');
    b.PlaceStone('A', 2, 'B');
    b.PlaceStone('A', 3, 'B');
    b.PlaceStone('B', 4, 'B');
    b.PlaceStone('C', 4, 'B');
    b.PlaceStone('D', 4, 'W');
    b.PlaceStone('B', 5, 'B');
    b.PlaceStone('C', 6, 'B');
    b.PlaceStone('D', 7, 'W');

    b.DisplayBoard();

    Player p;

    cout << p.CanCaptureIfPlaced(b, 'W', 15, 0);
    */
}


